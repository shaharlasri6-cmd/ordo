# Ordo v2.2.2

- Fixes stale launcher icon after app update.
- Uses standard Android launcher `mipmap-*` resources.
- Uses a new resource name to force launcher icon refresh.
- Both normal and round launcher icons point to the new Ordo icon.
