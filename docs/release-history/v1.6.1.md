# QuickContactsWidget v1.6.1

- Fixes call animation so widget taps no longer open an Activity.
- Uses a true system overlay above the launcher/home screen.
- Adds one-time "Display over other apps" permission setup in the app.
- Demo widget uses the same home-screen overlay without placing a call.
- Keeps the v1.6.0 in-app update checker and all prior widget improvements.
